<section style="background-image: url(/images/hello.jpg); background-repeat: no-repeat; background-size: cover;">
    <% if(!user){ %>
        <h1>Hello, Please sign in to access your profile</h1>
    <% } else { %>
        <h1>Welcome <%= user.username %>!</h1>
    <% } %>
</section>